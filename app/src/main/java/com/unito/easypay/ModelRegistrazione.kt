package com.unito.easypay

import com.unito.easypay.data.Result
import org.json.JSONObject
import java.io.IOException

class ModelRegistrazione() {
/*
    fun registrazione(data: JSONObject) : Result<String>{

        try{
            //Controllo che tutti i dati siano compilati
            if( data["nome"].toString().trim() == "" ||
                data["cognome"].toString().trim() == "" ||
                data["codicefiscale"].toString().trim() == "" ||
                data["telefono"].toString().trim() == "" ||
                data["email"].toString().trim() == "" ||
                data["password"].toString().trim() == "" ||
                data["confermapassword"].toString().trim() == ""
            )
            {
                throw Throwable("Compilare tutti i campi!")
            }else if(R.id.fieldPassword.toString() != R.id.fieldConfermaPassword.toString()){
                throw Throwable("Password errate!")
            }else{
                // Eseguo l'inserimento del nuovo utente

            }

        }catch (e : Throwable){
            return Result.Error(IOException("Error Sign In", e))
        }
    }

 */
}